[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "intent-engine"
version = "0.1.0"
description = "AI-Based System Intent Engine for Safe Linux Command Execution"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
  { name = "Harshit" },
  { name = "Praveen" },
  { name = "Vansh" },
]
keywords = ["linux", "shell", "safety", "ai", "llm", "command-interception"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: POSIX :: Linux",
  "Topic :: System :: Systems Administration",
]
dependencies = [
  "fastapi>=0.100.0",
  "uvicorn[standard]>=0.20.0",
  "pydantic>=2.0.0",
  "toml>=0.10.0",
  "structlog>=23.0.0",
  "httpx>=0.24.0",
  "rich>=13.0.0",
  "click>=8.0.0",
  "psutil>=5.9.0",
]

[project.optional-dependencies]
llm-ollama = ["ollama>=0.2.0"]
llm-gemini = ["google-generativeai>=0.7.0"]
llm-openai = ["openai>=1.30.0"]
dev = [
  "pytest>=8.2.0",
  "pytest-asyncio>=0.23.0",
  "pytest-cov>=5.0.0",
  "httpx>=0.27.0",
  "ruff>=0.4.0",
  "mypy>=1.10.0",
  "pre-commit>=3.7.0",
]

[project.scripts]
intent-daemon = "engine.daemon.server:run"

[tool.setuptools.packages.find]
where = ["."]
include = ["engine*", "rules*"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "ANN"]
ignore = ["ANN101", "ANN102"]

[tool.mypy]
python_version = "3.10"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--tb=short -v"
